<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.example.gloryBooks.dao.GloryCartDao">
    <select id="cartList" parameterType="int" resultType="GloryCart">
        SELECT*FROM glorycart WHERE user_id = #{userId}
    </select>
</mapper>